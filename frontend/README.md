
  # Financial Crime Detection UI

  This is a code bundle for Financial Crime Detection UI. The original project is available at https://www.figma.com/design/FTvxRoSyzKu17vRhNnNCls/Financial-Crime-Detection-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  